# App env example for local Next.js development
# Copy to `apps/extensions/.env.local` and adjust values as needed.

# Public site/base URL used for sitemaps and canonical links
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Option A: Develop without a database (fallback to JSON data)
SERP_EXTENSIONS_DISABLE_DB=1

# Option B: Use a real Postgres/Neon database (comment out the disable flag above)
# DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require
# (The app also accepts these alternative names if preferred):
# SERP_EXTENSIONS_DB_DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require
# POSTGRES_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require


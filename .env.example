# Root env example for local dev and tooling
# Copy to `.env` and adjust values as needed.

# 1) Local default: run without a DB (fallback to JSON)
SERP_EXTENSIONS_DISABLE_DB=1

# 2) Or use a real Postgres/Neon URL (uncomment and fill). Must include `?sslmode=require` on Neon.
# DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

# Alternative names also recognized by the DB client (optional):
# SERP_EXTENSIONS_DB_DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require
# POSTGRES_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

# Site base URL used by sitemap utilities (Next.js will read this from app-level env).
NEXT_PUBLIC_SITE_URL=http://localhost:3000

